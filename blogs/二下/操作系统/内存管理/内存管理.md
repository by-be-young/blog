---
type: 待完善
excerpt: 操作系统内存管理理论知识点，以及Lab2相关内容
series: 操作系统
order: "3"
chapter: 2-理论
---
# 为什么要内存管理？

## 实现地址空间的独立

> 设想，假如没有内存管理，程序自己控制内存空间。那么，程序编译完成后，每个变量或指令的地址就确定了。
> 
> 看似没有什么问题，但如果计算机同时运行两段程序呢？假如两段程序都要把指令放在同一个地址，那么不就冲突了吗？

所以，内存管理必须要让多段程序的地址空间相互独立，一个程序不能访问另一个程序的空间，这也提高了安全性。

## 内存分配与回收

必须通过覆盖、交换等方法释放不再需要使用的内存、把需要资源的载入内存。

## 地址转换（重定位）

> 既然程序的地址不能直接控制内存空间，那么说明程序给的地址和真实地址并不相同。那 CPU 如何找到真实的地址呢？

#### 程序编译链接后得到的地址是虚拟地址，实际执行时则根据物理地址操作。

### 地址空间

**地址空间是虚拟地址**（也叫逻辑地址）**的集合**。源程序经过编译之后得到的目标地址, 存在于它所限定的地址范围内，这个范围就是地址空间。

### 存储空间

**存储空间是物理地址的集合**。主存中一系列存储信息的物理单元，这些物理单元的地址就是物理地址。

### 重定位

将逻辑地址转换为物理地址的过程叫做重定位。

1. 静态重定位：程序加载时一次性完成，分为编译时重定位和装载时重定位；
2. 动态重定位：即执行时重定位，在每次内存访问的时候通过 MMU 等硬件动态实现地址转换。

[task]
[question]
关于重定位，错误的是（）。
[\question]
[options]A 重定位按照时机包含编译、装载和执行时重定位。[\options]
[options]B 重定位可能修改程序指令和数据的地址。[\options]
[options]C 装载时重定位依赖于 MMU 部件。[\options]
[options]D 执行时重定位会产生寄存器比较和相加的时间开销。[\options]
[answer]C[\answer]
[analysis]
- 选项 A 正确：重定位按时机分为编译时重定位（静态）、装载时重定位（装入时确定地址）和执行时重定位（运行时动态重定位）。
- 选项 B 正确：重定位的核心功能是将程序中的逻辑地址转换为物理地址，需要修改指令和数据中的地址信息。
- 选项 C **错误**：装载时重定位是在程序加载到内存时由**链接器/加载器**完成的地址转换，**不依赖 MMU**（内存管理单元）。MMU 主要用于执行时重定位（如分页、分段机制下的动态地址转换）。
- 选项 D 正确：执行时重定位（动态重定位）依赖 MMU 和基址寄存器（或页表），地址转换过程中会进行加法运算或比较操作，产生一定的时间开销。[\analysis]
[\task]

## 内存碎片整理

在进程的进进出出（分配回收）中，某一时刻的内存大概率会是：进程与进程之间有空隙。这种现象就是**碎片**。

如果空隙过多但每个空隙又很小，就会导致：明明容量是足够的，但是空闲的部分没有连在一起，导致没有地方再放入新进程。

因此，内存管理还需要整理碎片，并在分配内存的时候采用合理的策略。具体内容后面再细说。

# 内存管理的方式

通常有三种方式管理内存：分区、分段、分页。这三种方式是平行关系，但在实际中往往组合起来使用。

## 分区管理

### 核心思想

将内存划分为几个大小不一的区，每个进程独占一个区。

> - 固定式分区：每个区的大小固定
> 
> - 可变式分区：区与区之间的边界可移动

### 分配算法

1. 首次适应算法（First Fit）：每个空白区按其在内存空间中地址递增的顺序连在一起，在为作业分配内存区域时，从这个空白区域链的始端开始查找，选择第一个足以满足请求的空白块。
   
2. 下次适应算法（Next Fit）：把内存空间中空白区构成一个循环链，每次为内存请求查找合适的分区时，总是从上次查找结束的地方开始，只要找到一个足够大的空白区，就将它划分后分配出去。
   
3. 最佳适应算法（Best Fit）：为一个作业选择分区时，总是寻找其大小最接近于作业所要求的内存区域。
   
4. 最坏适应算法（Worst Fit）：为作业选择内存区域时，总是寻找最大的空白区。

> **作业和进程**
> 
> 一个进程通常源自一个作业。当作业被操作系统选中并调入内存分配资源后，它就“变身”为进程。

[task]
[question]
可变分区存储分配算法中，（）算法总是挑选可以容纳作业的最大的空闲区进行分配。
[\question]
[answer]
最坏适应（Worst Fit）[\answer]
[analysis]
最坏适应算法（Worst Fit）在分配内存时，总是从所有能够满足作业要求的空闲分区中挑选最大的一个进行分配。其目的是尽量让剩余的空闲区仍然足够大，以便后续能够容纳较大的作业，但同时也可能导致较大的空闲区被分割成较小的碎片。[\analysis]
[\task]

[task]
[question]
在一个交换系统中，按内存地址排列的空闲区大小是: 10 KB、4 KB、20 KB、18 KB、7 KB、9 KB、12 KB 和 15 KB。对于连续的段请求：12 KB、10 KB、9 KB。使用 First Fit、Best Fit、Worst Fit 和 Next Fit 将找出哪些空闲区？
[\question]
[answer]
| 算法 | 分配结果 |
|------|----------|
| First Fit | 20 KB, 10 KB, 18 KB |
| Best Fit | 12 KB, 10 KB, 9 KB |
| Worst Fit | 20 KB, 18 KB, 15 KB |
| Next Fit | 20 KB, 18 KB, 9 KB |
[\answer]
[analysis]
空闲区按地址顺序排列为：10 KB、4 KB、20 KB、18 KB、7 KB、9 KB、12 KB、15 KB。

- **First Fit**：12 KB→20 KB；10 KB→10 KB；9 KB→18 KB。
    
- **Best Fit**：12 KB→12 KB；10 KB→10 KB；9 KB→9 KB。
    
- **Worst Fit**：12 KB→20 KB；10 KB→18 KB；9 KB→15 KB。
    
- **Next Fit**：12 KB→20 KB；10 KB→从 20 KB 之后找→18 KB；9 KB→从 18 KB 之后找→9 KB。
[\analysis]
[\task]

### 缺点

由于一个进程占一个分区，在固定式分区下，很容易导致一个分区没有用满，这称为**内碎片**。而如果采用可变式分区，则能显著减少内碎片的问题。但相应地，因为不能整体移动进程，导致进程之间会有很多小的空分区，甚至难以放入一个进程，这就是**外碎片**。

## 分段管理

### 核心思想

由于程序本身就会在编译和链接后被划分为 `.data`、`.text` 等多个段，所以可以按照这个特点，将内存也划分为多个段，段内保持连续，但段与段之间不一定连续。

多个进程可以共享段，如共享库。但是，不同进程的 `.data` 段会放在内存的不同段中，以保持独立性。

### 地址转换方法

虚拟地址的结构为【 段号 | 段内偏移 】。

根据段号找到段表对应的段基址。段基址加上段内偏移即为物理地址。

### 缺点

有些可变长的段在分配回收的过程中，会产生段与段之间的**外碎片**。

[task]
[question]
采用分段内存管理方法会造成外部碎片。（）
[\question]
[options]A 正确[\options]
[options]B 错误[\options]
[answer]A[\answer]
[analysis]
分段内存管理将程序按逻辑结构分为多个段（如代码段、数据段、堆栈段），每个段的大小不同。由于各段大小不固定，且在内存中动态分配和释放，经过多次分配后内存中会留下许多无法被大作业利用的小空闲区域，即**外部碎片**。因此该说法正确。分页管理通过固定大小的页面可消除外部碎片，但会产生内部碎片。[\analysis]
[\task]

## 分页管理

分区和分段管理都还是会产生很大的碎片，如何才能让碎片最小化、最少化呢？

> Young 做了一个梦！梦见他变成了一只乌鸦，天气太热了，好想喝水！突然，他发现了一个装满水的玻璃瓶！好一个乌鸦喝水！
> 
> 玻璃瓶旁边有一堆大石子和一堆细沙。Young 由于听说过乌鸦喝水的故事，于是毅然决然选择了大石子，结果扔进去才发现，水全部填充在石子的空隙中了，石子塞满了水也没溢出！然后 Young 就这么渴死了！
> 
> 或许是老天爷的恩赐，Young 重生了，重生在他扔石子之前！这次他不能再渴死了，于是选择了堆细沙进去，果然水就涨起来了。
> 
> 所以说，要让水溢出去，就是要让填充物缝隙中的水尽量少，于是填充物选择细小的更好。Hmm，我能不能这么类比：瓶子是内存空间，填充物是进程，水是碎片。要让碎片尽量小，我只需要让进程尽量细碎化一点即可。怎么做？

### 核心思想

将内存空间和进程“切碎”成固定大小且等大的块，离散存放。

#### 物理内存块称为页框，进程地址空间块称为页。

页框和页的对应关系储存在页表中，记录每一页存放在哪一个页框里。支持分页的硬件部件叫做 MMU。

进程以页为单位装进内存。

> 这种方法大胆地打破了“进程地址空间必须连续”的限制，就像沙子一样，可以任意流动。

[task]
[question]
关于内存碎片，下面说法错误的是_ 。
[\question]
[options]A 固定分区可能产生内碎片[\options]
[options]B 可变分区可能产生外碎片[\options]
[options]C 请求分页系统不会产生碎片[\options]
[options]D 外部碎片可以通过紧凑技术来消除[\options]
[answer]C[\answer]
[analysis]
- 选项 A 正确：固定分区将内存划分为大小固定的分区，当作业小于分区大小时，分区内剩余空间无法被其他作业使用，产生**内部碎片**。
- 选项 B 正确：可变分区根据作业大小动态分配分区，经过多次分配释放后，内存中会留下许多难以利用的小空闲区域，产生**外部碎片**。
- 选项 C **错误**：请求分页系统以固定大小的页面为单位分配内存，不会产生**外部碎片**，但会产生**内部碎片**（进程最后一页可能未填满）。因此“不会产生碎片”的说法是错误的。
- 选项 D 正确：外部碎片可以通过**紧凑技术**（内存搬移）将分散的空闲区合并成一个大空闲区来消除。[\analysis]
[\task]

[task]
[question]
关于页表项结构的下列说法中，正确的是：
[\question]
[options]A 当操作系统把该页从物理内存调出时，会对有效位置位[\options]
[options]B 当硬件在写入一个页面时，硬件将该页的修改位置为 0[\options]
[options]C 页表项可用于实现页面到页框的映射[\options]
[options]D 页表项会保存对应页面的外存地址[\options]
[answer]C[\answer]
[analysis]
- 选项 A 错误：有效位表示页面是否在物理内存中。当页面被调出物理内存时，有效位应设置为 **0**（无效），而不是置位（1）。若页面在内存中，有效位为 1。
- 选项 B 错误：修改位（dirty bit）由硬件在写入页面时自动**置为 1**，表示该页内容已被修改。写完后不会置为 0，而是由操作系统在页面换出时判断。
- 选项 C **正确**：页表项的核心功能是将虚拟页面映射到物理页框（Page Frame），其中存储物理页框号（PFN）等信息。
- 选项 D 错误：页表项中**不保存**外存地址。当页面被换出时，外存地址通常保存在内存中的另一数据结构（如 swap 表或虚拟内存管理区域）中，而非页表项中。页表项此时仅标记有效位为 0。[\analysis]
[\task]

> **内存、外存**
> 
> 内存指的是物理内存，常储存在 RAM 中；
> 
> 外存指的是磁盘、磁带等。
> 
> 虚拟地址映射的是物理内存，而后续将讲到的[[#页面置换算法]] 指的是物理内存与外存之间的页面置换。

### 地址转换方法

虚拟地址的结构为【 页号 | 页内偏移 】。

根据页号找到页表对应的页框号。$物理地址 = 页框号 × 页大小 + 页内偏移$。

注意，和分段管理不同的是，分页管理的转换相当于移位拼接，而不是简单的加法。最终的物理地址的结构为【 页框号 | 页内偏移】。

### 缺点

若一页设计得很大，则很容易出现最后一页剩了很多空间没装满，即很大的**内碎片**。且由于要以页为单位装进内存，仍然会分配得不灵活。

若一页设计得很小，又会导致需要很大的页表。页表本身又会占据内存空间，太臃肿了。

[task]
[question]
以下说法正确的是：
[\question]
[options]A 两个不同进程对应的页表中可能包含内容相同的页表项[\options]
[options]B 虚拟地址空间总是大于物理地址空间[\options]
[options]C 在页式内存管理下，页面尺寸越小越有利于消除外碎片，提高内存使用效率[\options]
[options]D 在段式内存管理下，不同分段尺寸大小可以不同，从而可以消除外碎片[\options]
[answer]A[\answer]
[analysis]
- 选项 A **正确**：两个不同的进程可能通过共享内存机制映射到同一物理页框，此时两个进程页表中的对应页表项内容（物理页框号、权限等）相同。
- 选项 B 错误：虚拟地址空间不一定总大于物理地址空间。例如在 32 位系统中虚拟地址空间为 4 GB，若物理内存只有 1 GB，则虚拟地址空间大于物理；但在嵌入式系统中虚拟地址空间可能小于物理地址空间。
- 选项 C 错误：页面尺寸过小会导致页表项数量剧增，页表占用更多内存，同时磁盘 I/O 效率降低（每次传输小页面）。分页管理不存在外碎片。
- 选项 D 错误：段式管理允许分段尺寸不同，但仍会产生外碎片，因为各段大小不一，动态分配和释放后内存中会留下许多无法利用的小空闲区域。[\analysis]
[\task]

### 多级页表

为了平衡上述左右脑互搏的问题，**又要保证页设计得尽量小**以减小内碎片，我们可以采用多级页表的方法，来**减少页表占用的内存**。就是，给页表再来个页表！实际上，这个高级页表叫做页目录。或者，也可以把页目录称为一级页表，页表称为二级页表。

这样，虚拟地址的结构就被划分为【 一级页表偏移量 | 二级页表偏移量 | 页内偏移】。

以一级页表基地址为准，加上一级页表偏移量，找到对应的一级页表项。这个项的结构为【 二级页表基地址 | 权限位】。权限位用于判断该表项是否有效等。

然后根据这个二级页表基地址，加上二级页表偏移量，找到对应的二级页表项。这个项的结构为【 物理页号（页框号） | 权限位】。

最终，物理地址的结构为【 物理页号 | 页内偏移】。

[task]
[question]
关于多级页表，下列说法错误的是：
[\question]
[options]A 能够减少页表占用内存的大小[\options]
[options]B 级数越多，平均访问内存的时间越长[\options]
[options]C 有效的页表项中都会存储页框号[\options]
[options]D 使用二级页表的平均访存性能优于一级页表[\options]
[answer]D[\answer]
[analysis]
- 选项 A 正确：多级页表允许部分页表不在内存中（如未使用的虚拟地址区域无需分配底层页表），从而减少页表占用的总内存。
- 选项 B 正确：级数越多，地址转换时需要多次访问内存（每级一次），平均访存时间越长。
- 选项 C 正确：有效的页表项（即已映射到物理内存的页面）必须存储对应的物理页框号（PFN）。
- 选项 D **错误**：一级页表只需一次内存访问即可完成地址转换（TLB 缺失时）。二级页表需要两次内存访问（先访问页目录，再访问页表），平均访存性能**差于**一级页表。多级页表牺牲性能换取内存占用。[\analysis]
[\task]

[task]
[question]
如图，要想访问物理地址 `0x326028`，需要使用哪个逻辑地址？
![[内存管理题图-物理转逻辑.png]]
[\question]
[answer]
`0x00C01028`
[\answer]
[analysis]
物理地址 `0x326028` 对应的页框号为 `0x326000`，页内偏移为 `0x028`。根据图中信息，构造二级页表索引为1、页目录索引为3、页内偏移为 `0x028` 的逻辑地址，即：`(3<<22) | (1<<12) | 0x028 = 0x00C01028`。[\analysis]
[\task]

[task]
[question]
一个 64 位系统架构中内存采用 4 级页表管理，每个页面大小 8192 字节，每个页表项占 8 字节，每一级页表项恰好填满一页。
1. 页内偏移在虚拟地址中占几位？
2. 计算有效虚拟地址位长度和有效地址空间大小。
3. 画出虚拟地址结构，标出各个部分起止位置和长度。
4. 假设当前进程的第一级页表（页目录）起始物理地址为 0 x 4000，给出虚拟地址 0 xFFFFABC 123456789 对应的页目录表项的物理地址。
[\question]
[answer]
（1）**13 位**  
（2）**53 位，$2^{53}$ 字节**  
（3）虚拟地址结构（从高位到低位）：

| 一级索引 | 二级索引 | 三级索引 | 四级索引 | 页内偏移 |
|:-------:|:-------:|:-------:|:-------:|:-------:|
| 10 位 | 10 位 | 10 位 | 10 位 | 13 位 |
| 52~43 | 42~33 | 32~23 | 22~13 | 12~0 |

高 11 位（63~53）为符号扩展位。

（4） **0 x 5 FA 8**
[\answer]
[analysis]
**1. 页内偏移位数**：页面大小 $8192 = 2^{13}$ 字节，因此页内偏移占 **13 位**。

**2. 有效虚拟地址位长度和空间大小**：每级页表项数 = $8192 / 8 = 1024 = 2^{10}$，每级索引占 10 位。4 级页表共 $4 \times 10 = 40$ 位索引，加上 13 位页内偏移，有效虚拟地址总长度为 $40 + 13 = 53$ 位，有效地址空间大小为 $2^{53}$ 字节。

**3. 虚拟地址结构**：53 位有效地址中，高 40 位分为 4 级页表索引（每级 10 位），低 13 位为页内偏移。在 64 位系统中，高 11 位（63~53）为符号扩展位（全 0 或全 1）。

**4. 页目录表项物理地址**：
- 虚拟地址 `0xFFFFABC123456789`，取一级索引位（52~43）：右移 43 位后取低 10 位
- 计算结果：一级索引 = `0x3F5`
- 每个页表项占 8 字节，偏移量 = `0x3F5 × 8 = 0x1FA8`
- 页目录起始物理地址 `0x4000`，页目录表项物理地址 = `0x4000 + 0x1FA8 = 0x5FA8`
[\analysis]
[\task]

### TLB

> 你以为多级页表就是最佳答案了吗？其实不然。如果页表级数过多，每次地址转换的时间就会很长，效率极大下降。
> 
> 记得上学期计组学的 cache 吗？我们可以为页表也设计一个高速缓存，将部分映射关系存放在这里，就不用再一级一级地查找了！

TLB中不区分一级、二级页表偏移量，而是合成为“虚页号”。

即，虚拟地址的结构为【 虚页号 | 页内偏移】。

快表项的结构为【 虚页号 | 物理页号】。

和之前类似，得到物理地址为【 物理页号 | 页内偏移】。

> 如果你认为：这不是又回到了单级页表了吗？
> 
> 那么你或许需要先回去看看 cache 的相关知识点。

[task]
[question]
同一进程的线程之间的上下文切换需要刷新 TLB。（）
[\question]
[options]A 正确[\options]
[options]B 错误[\options]
[answer]B[\answer]
[analysis]
同一进程内的多个线程共享该进程的地址空间（即共享相同的页表）。因此，当从一个线程切换到另一个线程时，虚拟地址到物理地址的映射关系没有发生变化，TLB（旁路转换缓冲，即快表）中的条目仍然有效，**无需刷新 TLB**。只有当切换到不同进程的线程时，由于地址空间发生变化，才需要刷新 TLB。因此该说法错误。[\analysis]
[\task]

### 哈希页表

仍然是相同的虚拟地址结构【 虚页号 | 页内偏移】。

将虚拟页号通过哈希函数转化并查哈希表得到物理页号，再拼上页内偏移得到物理地址。

那么哈希页表的优势到底在哪呢？其实，我们可以用哈希函数将大页表缩小成小页表，这样能够大幅减小页表所用的空间。

但是这样自然而然就会想到**哈希冲突**的问题。为了解决，我们可以将哈希页表的表项设计成一个链表。这样，**哈希值**就包括：虚拟页号、物理页号、下一个元素的指针。

当虚拟页号不匹配时，再查找链表中的下一个元素即可。

### 反置页表

这个页表就非常有意思了。由于物理内存往往小于虚拟内存空间，那么用物理页号作为一大查页表的依据，将会缩小页表的大小，即页表大小只与物理内存空间大小有关。

而且，我们甚至可以让所有进程共用一张反置页表，因为表项中不涉及虚拟地址。

但是由于我们无法直接知道物理页号，因此需要额外建立一个**哈希表**。而且，当然不可能每个进程页表完全一致，每个进程需要有单独的哈希表，来将自己的虚拟地址独立地转化为物理地址，这说明我们还需要知道**该进程的 PID**。这个可以直接从 PCB 中获得。

[task]
[question]
反置页表（也称反向页表）为每个进程的每个虚拟页保留一个条目。
[\question]
[options]A 正确[\options]
[options]B 错误[\options]
[answer]B[\answer]
[analysis]
反置页表与传统的多级页表不同，它是**以物理内存为索引**，为**整个系统的每个物理页框**保留一个条目，而不是为每个进程的每个虚拟页保留条目。每个条目记录了哪个进程的哪个虚拟页正在映射到该物理页框。因此，条目数由物理内存大小决定（物理页框数），而非进程数或进程的虚拟页数。当进程数较多时，反置页表可大幅节省内存占用。因此该说法错误。[\analysis]
[\task]

> **相关术语中英对照表**
> 
> | 中文术语            | 英文全称                                     | 缩写             |
> | --------------- | ---------------------------------------- | -------------- |
> | **内存管理单元**​     | Memory Management Unit                   | **MMU**​       |
> | **快表**​         | Translation Lookaside Buffer             | **TLB**​       |
> | **页表**​         | Page Table                               | PT             |
> | **页表项**​        | Page Table Entry                         | **PTE**​       |
> | **页目录**​        | Page Directory                           | PD             |
> | **页目录项**​       | Page Directory Entry                     | **PDE**​       |
> | **虚页号**​        | Virtual Page Number                      | **VPN**​       |
> |**一级页表偏移量/页目录索引**|Page Directory Entry Index| **PDX**|
> |**二级页表偏移量/页表索引**|Page Table Entry Index|**PTX**|
> | **页框号 / 物理页号/物理帧号**​ | Page Frame Number / Physical Page Number | **PFN / PPN**​ |
> | **页内偏移**​       | Page Offset                              | Offset         |

---

[task]
[question]
关于快表错误的是_ 。
[\question]
[options]A 使用快表能使得页表查询速度更快[\options]
[options]B 快表的命中率越高，访存的速度越快[\options]
[options]C 反置页表无法使用快表加速[\options]
[options]D 快表项需要记录物理块号及其对应的虚拟页号[\options]
[answer]C[\answer]
[analysis]
- 选项 A 正确：快表（TLB）是位于 CPU 内部的硬件缓存，存储最近使用的页表项，可以避免每次地址转换都访问内存中的页表，显著提高查询速度。
- 选项 B 正确：快表命中率越高，地址转换时访问 TLB 成功的概率越大，越能减少对内存页表的访问，访存速度越快。
- 选项 C **错误**：反置页表同样可以使用快表加速。反置页表以物理页框为索引，虽然查询方式与传统页表不同，但 TLB 仍可缓存其常用的地址转换映射关系，并不存在无法使用快表加速的问题。
- 选项 D 正确：快表项本质上是页表项的缓存，需要记录虚拟页号（VPN）和对应的物理页框号（PFN）以及权限位等信息。[\analysis]
[\task]

# 自映射【!】

## 目的

我们知道，页表是存储在物理地址空间中的。假如内核需要维护（读写）页表，那么我们需要知道页表的物理地址。但是用户态程序**不能直接获得页目录的物理地址**！只能通过虚拟地址查页表才能找到物理地址，这样就陷入了个死循环：

#### 需要读写页表，必须找到物理地址；要找到物理地址，必须读页表。

为了解决这个问题，MOS 采用了一种叫做“自映射”的方法，用一部分虚拟地址空间，映射到页表的物理地址空间。

这样还不够，因为你必须要在页目录里查，而页目录的物理地址还是不知道！所以，需要能够通过某些虚拟地址，按照正常的查页表方法，能够**找到页目录本身**，并通过 Offset 查到页目录中的任意一项。

## 实现方法

我们已经知道，虚拟地址划分为了三个部分：

【PDX|PTX|Offset】

我们要做的是能让这个虚拟地址直接找到页目录本身，**而页目录本身是一个特殊的页表**，所以页目录中一定有一项存储的就是页目录自身的物理地址。

设页目录的第 k 项是页目录自身，那么 **PDX 必须为 k**；

这还不够，还要再查一次，并且还是得要获取页目录自身。也就是说，我们已经查到了页目录，再要根据页目录查页目录本身。那么页目录的第几项是页目录本身呢？还是 k！所以，**PTX 还是必须为 k**。

这样一来，改变 Offset 的值，就能查询页目录中的每一个项（即每一个页表）了。

**既然 PDX 和 PTX 是固定的**，且由于 Offset 共 12 位，所以可以有 $2^{12}=4 KB$ 的虚拟地址空间来映射页目录中的所有项。

如果不想找到页目录，只想找某一个页表，那可以改变 PTX，这样就是正常查页表而非自映射了。共 22 位可变，也就是 $2^{22}=4 MB$。所以，

#### 共有 4 MB 的虚拟地址空间映射到 4 MB 的所有页表所在的物理地址空间。

更进一步地，如果连页表都不想找，只想找一个页，那连 PDX 都可以改变，这样总共有 $2^{32}=4 GB$，于是物理地址空间最多有 4 GB。（我们又从自映射返回到了普通分页管理！）

> **为什么所有页表所在的物理地址空间也是 4 MB**
> 
> - 从位数的角度来看
> 
> 一个页的大小为 4 KB，可以通过 Offset 的位数计算出来。
> 
> 而页表本身也是一个页，因此一个页表的大小也是 4 KB。
> 
> 共有 1024 个页表，可以通过 PDX 的位数计算出来。
> 
> 所以共需要 $1024*4 KB=4 MB$ 的物理地址空间。
> 
> - 从映射的角度来看
> 
> 由于一个虚拟地址要映射到一个物理地址，要映射到 4 MB 大小的物理地址空间，则必须要不小于 4 MB 的虚拟地址空间。
> 
> 在 MOS 中，我们用刚好 4 MB 的空间来映射，当然是合适的。
> 
> 在 Linux 等现代操作系统中，往往会留出更多的虚拟地址空间来映射页表，因为 Linux 的内核页表可能会动态扩展。

### 小结

| **情况** | **PDX 固定** | **PTX 固定等于 PDX** | **效果**             |
| ------ | ---------- | ---------------- | ------------------ |
| 1      | 是（特殊值 k）   | 是                | 访问**页目录自身**（自映射模式） |
| 2      | 是（指定值 p）   | 否                | 访问**第 p 个页表**​     |
| 3      | 否          | 否                | 访问任意物理页（普通内存访问）    |

## 具体方案

可以在虚拟地址空间中确定一个 4 MB 的空间，且低 22 位为 0（即只有 PDX 部分有数字，这样不会影响其他）。设这个空间的起始地址为 `PTbase`，则映射到页目录的虚拟地址为 `(PTbase) | (PTbase >> 10)`，以保证 PTX 与 PDX 相等。

### 通过 `Offset` 查页目录

我们通过 `PDX` 和 `PTX` 查到页目录后，需要用 `Offset` 进行页内偏移查找页目录项，以得到页表。

注意，`Offset` 是**按字节寻址**的！比如，你要查页目录项中页目录本身（即页目录第 k 项），则 `Offset` 应该为 $k*4$ 才对。即：` PTbase|(PTbase)>>10| (PTbase)>>20 `。（可以看作右移 22 位得到 k，然后再左移 2 位，相当于 $*4$）。

[task]
[question]
一个进程如果从虚拟地址 `0x77800000` 开始映射 `4MB` 大小页表空间，求第一级页表（页目录）的起始逻辑地址，并指出从哪个逻辑地址可以读出第一级页表（页目录）所在的物理页框号。说明理由。（注意B代表字节，一个32位地址占4字节）
对于32位地址字长，2级页表，`4KB` 页面大小，虚拟地址 `va`，一级页表索引10位，二级页表索引10位，页内偏移12位。
[\question]
[answer]
- 页目录起始逻辑地址：**`0x779DE000`**
- 读出页目录物理页框号的逻辑地址：**`0x779DE778`**
[\answer]
[analysis]
页表空间从0x77800000开始，每个页表项4字节，共1024个二级页表。页目录本身也是一个页表，其虚拟地址的高10位（一级索引）与自身在页目录中的索引相同，因此页目录起始地址 = `(PTE_BASE) | (PTE_BASE >> 10) = 0x77800000 | 0x1DE000 = 0x779DE000`。
页目录自映射表项地址 = 页目录起始地址 | `(PTE_BASE >> 20) = 0x779DE000 | 0x778 = 0x779DE778`，该地址存储页目录自身的物理页框号。[\analysis]
[\task]

# 页面置换算法

当进程所访问的页面不在内存中时，便会向 CPU 发出缺页中断，中断相应后便转入缺页中断处理程序，将一个空闲页调入内存中。

但是，如果内存中已经没有空闲空间了，则需要用一定的**页面置换算法**，将内存中的某一页换出到外存。

[task]
[question]
关于程序的局部性原理，错误的是：
[\question]
[options]A 局部性原理包括时间局部性和空间局部性[\options]
[options]B 进程调度的有效运转依赖程序的局部性原理[\options]
[options]C 程序中的循环结构会导致程序的时间局部性[\options]
[options]D 主存-辅存机制的有效运转依赖程序的局部性原理[\options]
[answer]B[\answer]
[analysis]
- 选项 A 正确：局部性原理包括时间局部性（最近访问的数据可能再次被访问）和空间局部性（邻近的数据可能被访问）。
- 选项 B **错误**：进程调度（如时间片轮转、优先级调度）主要依赖时钟中断和调度算法，与局部性原理无直接关系。局部性原理主要影响缓存、虚拟内存、TLB 等性能优化机制。
- 选项 C 正确：循环结构会反复访问同一段指令和数据，体现出时间局部性。
- 选项 D 正确：虚拟内存的主存-辅存机制（如页面置换）依赖局部性原理，否则会发生频繁的页面换入换出（抖动）。[\analysis]
[\task]

## 最优置换算法（OPT）

淘汰的页面为**以后永不使用**的页面，或**最长时间不再被访问**的页面。

这显而易见拥有最低的页错误率，但是这就要求我们必须知道后续的引用串长什么样，这在实际算法中是不可能实现的。

因此，该算法只适合用于比较研究其他算法。

## 先进先出算法（FIFO）

淘汰的页面为**最早进入内存**的页面。

这个算法最为简单，就是普通的队列。但它没有用到任何的局部性原理，性能较差。

> **Belady 异常**
> 
> 有时候，为一个进程分配更多的物理块（如一开始 3 个，增加到 4 个），缺页次数反而增加的现象，称为 Belady 异常。
> 
> 这种现象在 FIFO 算法中有可能出现，但在 OPT、LRU 等算法中是不可能的。

为了优化 FIFO 算法，产生了一系列变种：

### 二次机会算法（Second-Chance）

淘汰的页面为**最近没有引用过**且**最早进入内存**的页面。

具体的实现方法为：给每个页面加个访问标志位，如果在加入队列后已经被再次访问过，则标志位改为 1。

然后在需要置换时，仍然和 FIFO 一样从队尾开始，如果标志位为 1，则改为 0，移到队头；否则直接选取该页面。循环直到找到可用的页面。

> “二次机会”体现在：如果该页面被用过了，则再给它一次机会，重新排到队头。
> 
> 但是如果所有的页面都被用过了，则最终结果就和 FIFO 没有任何区别了。因为所有人都被给了第二次机会。

[task]
[question]
第二次机会算法选择最近有引用的页面作为被淘汰页面。
[\question]
[options]A 正确[\options]
[options]B 错误[\options]
[answer]B[\answer]
[analysis]
第二次机会（Second Chance）页面置换算法是时钟算法的变体。它会检查每个页面的引用位：如果引用位为 1（最近有引用），则清除引用位并给该页面第二次机会（移动到队列尾部），然后继续检查下一个；只有当引用位为 0（最近没有引用）时，才选择该页面进行淘汰。因此该算法淘汰的是**最近没有引用**的页面，而非最近有引用的页面。所以说法错误。[\analysis]
[\task]

### Clock 算法（最近未用算法、NRU）

淘汰的页面为**最近没有引用过**且**最早进入内存**的页面。

从结果上看（也就是淘汰序列）和二次机会算法没有任何区别，只是实现上是二次机会算法的优化：

采用循环队列的方法，如果当前指向的页面标志位为 1，则改为 0，但不移到队头，而是直接将指针移到下一个页面。

[task]
[question]
某进程运行时依次访问的内存页面为：1, 2, …, n, 1, 2, …, n, 1, 2, …, n（连续 3 次，按顺序访问 1~n 号页面，n>3）。采用 FIFO 算法进行页面置换，共为该进程分配了 n-1 个页框，初始时这 n-1 个页框均为空，则完成上述内存页面访问会产生\_\_\_\_次缺页中断；如果采用 OPT 算法进行页面置换，其他条件不变，则会产生\_\_\_\_次缺页中断。
[\question]
[answer]
| 算法 | 缺页次数 |
|------|---------|
| FIFO | 3 n |
| OPT | n+2 |
[\answer]
[analysis]
访问序列长度为 3 n，页框数 m = n-1（n>3）。

**FIFO 算法：**

一般规律：第一次遍历缺页 n 次，第二次遍历缺页 n 次，第三次遍历缺页 n 次，总计 **3 n**。

**OPT 算法：

一般规律：第一次遍历缺页 n 次，第二次遍历仅缺页第 n 页 1 次，第三次遍历仅缺页第 n 页 1 次，总计 **n+2**。
[\analysis]
[\task]

## 最近最久未使用算法（LRU）

淘汰的页面为**最近最长时间未使用**的页面。

这种算法需要给页面设置一个访问字段，用于记录页面自上次访问以来所经历的时间。

> 这个方法理论上性能最好，最接近 OPT 算法的缺页率。但是，真正实现起来的时候，对维护的要求较高，需要硬件的支持，开销较大。

> **抖动问题**
> 
> 将**最近一段时间内，进程正在频繁使用的那些页面的集合**称为工作集。
> 
> 如果工作集中的页面数大于可用的物理页框数（驻留集）时，就会导致系统抖动：缺页率大幅上升。
> 
> 也就是说，驻留集大小越接近工作集大小，缺页率越小。

[task]
[question]
在工作集模型中，当所有工作集的页面数高于可用的物理页框数就会导致系统抖动。
[\question]
[options]A 正确[\options]
[options]B 错误[\options]
[answer]A[\answer]
[analysis]
工作集模型定义了进程在某个时间段内频繁访问的页面集合。当系统中所有进程的工作集大小之和超过可用的物理页框总数时，操作系统无法为每个进程提供足够的内存，导致进程频繁发生缺页中断，不断进行页面换入换出，CPU 忙于处理缺页而实际执行用户程序的时间很少，这种现象称为**系统抖动（thrashing）**。因此该说法正确。[\analysis]
[\task]

# MOS具体实现方法

## 概览

### CPU 访存流程

```mermaid
flowchart TD
    A[CPU发出访存指令] --> B[硬件查询TLB]
    B --> C{TLB命中?}
    C -->|是| D[访问物理内存]
    C -->|否| E[查找页表项]
    E --> F{页表项状态?}
    F -->|有效| G[TLB重填]
    F -->|无效或不存在| H[缺页异常处理]
    H --> I[分配空闲页]
    I --> J[插入/更新页表项]
    J --> G
    G --> B
```

### 内存回收流程

```mermaid
flowchart TD
    A[进程退出/内存回收] --> B[释放页表项<br/>解除虚拟-物理映射]
    B --> C[将页帧放回空闲链表]
```

## 初始化内存管理

### `mips_detect_memory(u_int _memsize)` 检测可用内存

```C
void mips_detect_memory(u_int _memsize) {
    memsize = _memsize;
    npage = memsize / PAGE_SIZE;
    printk("Memory size: %lu KiB, number of pages: %lu\n", memsize / 1024, npage);
}
```

- 参数 `_memsize` 从哪来？

一般来说，在 bootloader 阶段，通过特定的寄存器传递内存信息。

- `npage` 是什么？

物理页的总数。由于我们将内存划分为大小相等的多个页，故用总内存空间除以页大小即可。

这个 `PAGE_SIZE` 在 `mmu.h` 头文件中有定义。

### `mips_vm_init()` 初始化

```C
void mips_vm_init() {
    pages = (struct Page *)alloc(npage * sizeof(struct Page), PAGE_SIZE, 1);
    printk("to memory %x for struct Pages.\n", freemem);
    printk("pmap.c:\t mips vm init success\n");
}
```

#### `pages` 是一个数组，解引用得到第一个 `Page`。

> **`struct Page`**
> 
> 页**控制块**的结构体，里面记录的是某一个物理页的信息。通过这个可以管理物理内存的分配。
> 
> 其定义可在 `pmap.h` 头文件中查看。
> 
> - `pp_ref` 字段：表示这一页被引用了多少次。换句话说，有几个虚拟页被映射到了这一个物理页。
> 
> - `pp_link` 字段：与链表有关的结构体，后续详细讲解。

-  `alloc` 函数：

```C
void *alloc(u_int n, u_int align, int clear) {
    extern char end[];	// 链接器定义的符号，内核镜像结束后的第一个地址
    u_long alloced_mem;

    if (freemem == 0) {
        freemem = (u_long)end;
    }

    freemem = ROUND(freemem, align);	// 地址对齐
    alloced_mem = freemem;
    freemem = freemem + n;
    
    panic_on(PADDR(freemem) >= memsize);
    
    if (clear) {
        memset((void *)alloced_mem, 0, n);
    }
    
    return (void *)alloced_mem;
}
```

1. `freemem` 变量是什么？

	是一个地址，指的是未被分配的内存从这里开始。

2. 返回值 `alloced_mem` 有什么意义？

	这个返回值表示我们这次操作分配的内存的起点。实际上，这个就是第一个 `Page` 的地址，也就是 `pages` 数组的地址。

## 物理内存管理

### 概览

MIPS 框架下的操作系统（MOS）采用**分页管理**。

我们需要知道哪些物理页放入了页表中（即**有虚拟页与其映射**）、又有哪些页是**空闲的**（即**没有虚拟页与其映射**）。

#### MOS 采用了双向链表法管理空闲的页框。

> 需要注意的是：
> 
> 链表里面存储的仅有**空闲的**页框的**控制块**，即每个节点都是 `struct Page` 结构体；而 `pages` 数组里面是按顺序存储**所有**页框的控制块，无论是否被引用。
> 
> 所以，链表内的各个节点都是 `pages` 数组的元素。

我们需要做的事情有：

1. 初始化物理页管理；
2. 将一个空闲页分配出去；
3. 减少引用次数甚至回收到空闲页链表中。

### 链表结构与宏

![[链表结构.jpg]]

我们的代码中有这个变量：

```C
struct Page_list page_free_list;
```

也就是说，`page_free_list` 是整个链表结构的名字，严格来说应该是链表的头指针 `lh_first` 所在的**结构体的**名字。

> 注：下列宏中的 `field` 在调用的时候应该对应 `pp_link`；
> 
> `head` 在调用的时候应该对应 `&page_free_list`。

- 宏 `LIST_FIRST(head)`：获取第一个节点地址

```C
page_free_list.lh_first;
// 或者
&page_free_list->lh_first;
// 使用宏
LIST_FIRST(&page_free_list);
```

- 宏 `LIST_NEXT(elm, filed)`：获取该节点的下一个节点

```C
#define LIST_NEXT(elm, field) ((elm)->field.le_next)
```

我个人不推荐用这个宏，很容易搞混。但是遇到的时候得要会展开。

- 宏 `LIST_INSERT_AFTER(listelm, elm, field)`：插入到某一节点后面

```C
#define LIST_INSERT_AFTER(listelm, elm, field) \
    do { \
        (elm)->field.le_next = (listelm)->field.le_next; \
        if ((listelm)->field.le_next != NULL) { \
            (listelm)->field.le_next->field.le_prev = &(elm)->field.le_next; \
        } \
        (listelm)->field.le_next = (elm); \
        (elm)->field.le_prev = &(listelm)->field.le_next; \
    } while (0)
```

#### 特别注意： 仔细看上面我画的图，`le_next` 解引用后就是下一个节点，是整个页控制块，而 `le_prev` 解引用后得到上一个节点的 `le_next`。所以说 `le_prev` 是指针 `le_next` 的指针。

调用方法：`listelm` 和 `elm` 都是**页控制块的指针**。`elm` 对应的 `Page` 插入到 `listelm` 这个已经在链表中的 `Page` 的后面。所以，应该是以 `&pages[x]` 的形式作为参数。**别少了取址符**。

> 宏定义里大量使用小括号的原因是：由于宏定义是直接字符串替换，如果参数本身包含运算符，可能会出现优先级问题。

- 宏 `LIST_INSERT_BEFORE(listelm, elm, field)`：插入到某一节点前面

```C
#define LIST_INSERT_BEFORE(listelm, elm) \
    do { \
        (elm)->pp_link.le_prev = (listelm)->pp_link.le_prev; \
        (elm)->pp_link.le_next = (listelm); \
        *(listelm)->pp_link.le_prev = (elm); \
        (listelm)->pp_link.le_prev = &(elm)->pp_link.le_next; \
    } while (0)
```

在前面插入就不需要考虑指针为空的问题了。

- 宏 `LIST_INSERT_HEAD(head, elm, field)`：插入为第一个节点

```C
#define LIST_INSERT_HEAD(head, elm, field) \
    do { \
        if (((elm)->field.le_next = (head)->lh_first) != NULL) \
            (head)->lh_first->field.le_prev = &(elm)->field.le_next; \
        (head)->lh_first = (elm); \
        (elm)->field.le_prev = &(head)->lh_first; \
    } while (0)
```

- 宏 `LIST_REMOVE(elm, field)`：删除某一节点

```C
#define LIST_REMOVE(elm, field) \
    do { \
        if (LIST_NEXT((elm), field) != NULL) \
            LIST_NEXT((elm), field)->field.le_prev = (elm)->field.le_prev; \
        *(elm)->field.le_prev = LIST_NEXT((elm), field); \
    } while (0)
```

### 物理内存管理函数

- `page_init()`
- `page_alloc(struct Page **pp)`
- `page_decref(struct Page *pp)`
- `page_free(struct Page *pp)`

## 虚拟内存管理

### 地址转换函数

| 函数名        | 功能描述         | 输入                      | 输出                  |
| ---------- | ------------ | ----------------------- | ------------------- |
| `page2ppn` | 页控制块地址转页框号   | `struct Page *pp`       | `u_long` 页框号        |
| `page2pa`  | 页面结构体转物理地址   | `struct Page *pp`       | `u_long`物理地址        |
| `pa2page`  | 物理地址转页面结构体   | `u_long pa`             | `struct Page *`页面指针 |
| `page2kva` | 页面结构体转内核虚拟地址 | `struct Page *pp`       | `u_long`内核虚拟地址      |
| `va2pa`    | 虚拟地址转物理地址    | `Pde *pgdir, u_long va` | `u_long`物理地址        |

### 虚拟内存管理函数

| 函数签名                                                                              | 功能描述           | 参数说明                                                                                  | 返回值                       |
| --------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------- | ------------------------- |
| `int page_insert(Pde *pgdir, u_int asid, struct Page *pp, u_long va, u_int perm)` | 建立虚拟地址到物理页面的映射 | `pgdir`: 页目录指针  <br>`asid`: 地址空间ID  <br>`pp`: 物理页面指针  <br>`va`: 虚拟地址 <br>`perm`: 权限标志 | 成功返回0，失败返回错误码 `-E_NO_MEM` |
| `struct Page *page_lookup(Pde *pgdir, u_long va, Pte **ppte)`                     | 查询虚拟地址对应的物理页面  | `pgdir`: 页目录指针  <br>`va`: 虚拟地址 <br>`ppte`: 用于获取二级页表项指针的空指针                            | 成功返回页面指针，失败返回NULL         |
| `void page_remove(Pde *pgdir, u_int asid, u_long va)`                             | 移除虚拟地址的映射      | `pgdir`: 页目录指针  <br>`asid`: 地址空间ID  <br>`va`: 虚拟地址                                    | 无返回值                      |

